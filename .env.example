# Copy to .env (gitignored) and fill in. Only POST /api/transcript needs this —
# the rest of the app and the entire test suite run without it.
# Get a key: https://openrouter.ai/keys
OPENROUTER_API_KEY=
