# Runtime dependencies. Kept identical to api/requirements.txt (Vercel's function
# builder reads the copy next to the function). Dev-only tools (uvicorn, pytest,
# httpx) live in requirements-dev.txt.
fastapi>=0.115
pydantic>=2.9
PyYAML>=6.0
openai>=1.50
python-dotenv>=1.0
