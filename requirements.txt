# Runtime dependencies live in api/requirements.txt (that's the one Vercel's
# serverless function builder installs). This file re-exports them so that
# `pip install -r requirements.txt` at the repo root still works for local dev
# and CI. Dev-only tools (uvicorn, pytest, httpx) are in requirements-dev.txt.
-r api/requirements.txt
